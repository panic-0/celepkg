# CelePkg 使用说明

这份说明面向日常使用 CelePkg 管理 Celeste 地图和 Mod 的玩家。

## 快速开始

1. 打开 CelePkg。
2. 在顶部输入或确认 Celeste 安装目录，例如：

```text
D:/SteamLibrary/steamapps/common/Celeste
```

3. 点击刷新按钮扫描当前目录。
4. 如果刚更新了地图文件、升级了 CelePkg，或统计看起来仍是旧结果，点击“刷新缓存并重新扫描地图”。

CelePkg 会扫描官方地图、`Mods` 目录、存档文件和依赖关系。官方地图只用于查看统计，不会被禁用。

## 顶部工具栏

顶部工具栏用于连接游戏目录、刷新扫描结果和启动游戏。

- 路径输入框：当前 Celeste 安装目录。
- 刷新：使用缓存快速读取当前结果。
- 刷新缓存并重新扫描地图：忽略旧缓存，重新读取地图包和统计。
- 启动：使用当前选择的地图 Profile 和 Mod Profile 启动 Celeste。
- 指标：显示启用地图数、启用 Mod 数、完成地图数和警告数。

## 地图

地图页展示官方地图和 Mod 地图包。

常用信息：

- 名称、文件名和版本。
- 小图数量。
- 完成状态。
- 死亡数、用时、草莓。
- 依赖警告。

常用操作：

- 启用 / 禁用地图。
- 收藏地图。
- 保护地图，避免 Profile 操作改动它。
- 点击地图进入详情。

地图详情包含：

- 概览：死亡、用时、草莓、心和磁带。
- 小图：按目录浏览小图，查看每张小图的统计。
- 依赖 / 文件：查看依赖、SID 和文件路径。
- 存档来源：查看统计来自哪些存档。

## 其他 Mod

其他 Mod 页用于管理非地图包，例如 Helper、皮肤、功能 Mod，以及只包含测试地图的代码 Mod。

常用操作：

- 启用 / 禁用 Mod。
- 收藏 Mod。
- 保护 Mod。
- 查看依赖和被地图引用的情况。
- 筛选“不被依赖”的 Mod，辅助清理不常用内容。

## Profile

Profile 用来保存一组启用状态。

CelePkg 分为两类 Profile：

- 地图 Profile：保存启用的地图，以及地图需要的测试图 Mod。
- Mod Profile：保存启用的其他 Mod。

常用操作：

- 保存当前启用状态为 Profile。
- 更新已有 Profile。
- 重命名 Profile。
- 删除不再需要的 Profile。
- 应用 Profile。
- 应用并启动游戏。

应用 Profile 时，CelePkg 会写入 `Mods/blacklist.txt`。受保护的条目不会被 Profile 直接启用或禁用。

## 显示设置

显示设置和地图、其他 Mod、Profile 同级。

这里可以调整：

- 草莓分母：
  - 概览分母：默认选项，贴近游戏章节概览显示。官图总分母为 175。
  - 全部草莓：包含金莓、无冲金、月莓、银莓、彩虹莓等特殊莓。官图完整总数为 202。
- 警告栏：控制地图 / Mod 列表是否显示警告数量列。
- 存档：选择哪些数字存档参与统计，支持多选。

草莓已收集数来自存档统计，因此可能出现类似官图 `24/20` 的情况：分子包含已收集的特殊莓，默认分母仍是游戏概览分母。

## 备份还原

备份页用于保护 CelePkg 会修改的游戏文件。

CelePkg 只备份和还原这些文件：

- `Mods/blacklist.txt`
- `Mods/favorites.txt`

备份目录位于：

```text
<Celeste>/celepkg/backups/
```

建议保持“修改前自动备份”开启。这样在应用 Profile、修改收藏等操作前，CelePkg 会先创建备份。

## 筛选和排序

地图和 Mod 页面左侧都有筛选区。

地图筛选支持：

- 搜索地图名称、文件名或 SID。
- 按启用状态筛选。
- 按完成状态筛选。
- 只看有存档统计的地图。
- 只看有警告的地图。
- 按名称、死亡数、用时、草莓排序。
- 显示或隐藏 Helper 测试图。

Mod 筛选支持：

- 搜索 Mod 名称或依赖。
- 按启用状态筛选。
- 只看有警告的 Mod。
- 只看不被依赖的 Mod。

## 文件写入位置

CelePkg 会写入的游戏目录文件：

- `<Celeste>/Mods/blacklist.txt`
- `<Celeste>/Mods/favorites.txt`
- `<Celeste>/celepkg/backups/`

应用配置和扫描缓存位于系统配置目录下的 `celepkg` 文件夹。扫描缓存可以重新生成。

## 常见问题

### 为什么官方地图草莓不是 202？

默认显示的是游戏概览分母，官图为 175。切到“显示设置 -> 草莓 -> 全部草莓”后，会使用包含特殊莓的完整分母 202。

### 为什么会出现 24/20？

这是游戏统计口径导致的正常情况。已收集数会包含特殊莓，但默认分母是概览分母。

### 改了地图文件后统计没变怎么办？

点击顶部“刷新缓存并重新扫描地图”。

### Profile 会不会删除我的 Mod？

不会。Profile 通过写入 `Mods/blacklist.txt` 控制启用状态，不会删除 Mod 文件。

### 备份会备份整个 Mods 文件夹吗？

不会。CelePkg 只备份它会修改的 `blacklist.txt` 和 `favorites.txt`。
