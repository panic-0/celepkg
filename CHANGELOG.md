# Changelog

## v0.7.1 - 2026-06-07

### 新增

- 地图和 Mod 详情页标题区新增“打开所在位置”，可从详情页直接定位当前本地内容。
- 左侧栏下载管理入口新增任务进度计数，显示安装完成、下载完成和任务总数。
- 地图详情“小图”页新增内置筛选区，支持小图名称 / SID 搜索、排序、倒序、难度分组和仅搜索当前层级。

### 改进

- 左侧栏按内容管理、获取与安装、维护重新分组，并把当前地图 Profile、Mod Profile、启用数量和依赖数量整合到侧栏摘要。
- Mod 获取列表压缩行高并突出 Mod 名称，来源、安装状态等标签更靠后显示。
- Profile 复制相关文案改为“克隆”，新克隆 Profile 默认使用 `Clone` 后缀。
- 统一文档里的“Mod 获取”命名，减少“下载 Mod”和“Mod 获取中心”混用。

## v0.7.0 - 2026-06-07

### 新增

- 本地内容和 Mod 获取中心搜索改为多字段匹配，支持名称、版本、文件名、相对路径、作者、描述、SID、依赖和目录来源信息，并在可见字段中高亮命中片段。

### 改进

- 加快依赖检测等待流程，减少更新和安装前重复计算依赖带来的卡顿。
- 桌面界面去除手机窄屏布局和纯读屏辅助语义，保留桌面窗口下的布局与鼠标操作体验。
- 内置官方内容的启用开关改为真正禁用状态，避免“看起来不能点但点击后才提示”的交互不一致。
- 发布流程收紧附件筛选，避免签名文件、压缩包和 `latest.json` 之外的无关文件进入 GitHub Release。

### 修复

- 修复启动参数解析，支持带空格的引号参数、转义字符、空引号和未闭合引号场景。
- 修复下载完成后遇到暂停或取消时可能遗留 staged 下载文件的问题。
- 收紧打开本地内容和备份位置的后端校验，只允许打开当前 Celeste 的 `Mods`、`Content` 和合法备份快照目录。

## v0.6.0 - 2026-06-07

### 新增

- 安装或更新 Mod 前显示依赖树预览，区分必需依赖、可选依赖、已满足、版本不足、缺失和循环依赖状态。
- 目录安装和本地更新流程支持先预览依赖处理计划，再确认是否继续加入下载任务。

### 改进

- 加快依赖检测流程，减少重复读取目录和依赖信息带来的等待。
- 本地内容筛选栏与下载 Mod 页统一样式，搜索框移动到列表上方，地图和其他 Mod 共用搜索内容但保留各自筛选选项。
- 优化左侧 Profile 摘要展示，让当前地图 Profile、Mod Profile 和启用数量更容易扫读。
- 拆分扫描缓存逻辑，并补充前端回归、依赖树和发布脚本测试。
- 发布流程新增版本、仓库、更新端点和更新清单校验，减少发布产物元数据错误。

### 修复

- 将 `bitsbolts/demo` 这类 `demo` 路径地图识别为测试图，避免带演示地图的代码 Mod 误入地图列表。

## v0.5.1 - 2026-06-06

### 新增

- 支持应用内检查 CelePkg 更新，并提供下载、安装与重启入口。
- 扫描本地内容时显示坏压缩包警告，便于定位无法读取的 Mod 文件。

### 修复

- 修复草莓统计口径，避免旧存档或跨地图草莓记录导致已收集数量超过当前地图总数。
- 固定仓库文本文件使用 LF 换行，避免 Windows 本地格式检查因 Git 自动换行失败。
- 修复发布签名配置，确保自动更新签名产物和 `latest.json` 正常生成。

## v0.5.0 - 2026-06-06

### 新增

- 本地内容支持打开对应文件或文件夹位置，便于快速定位已安装地图和 Mod。
- 本地内容详情显示可用更新的版本变化，更新前能直接看到当前版本与目标版本。
- 侧栏新增 Profile 快捷入口，常用 Profile 操作更容易触达。

### 改进

- 拆分前端流程、应用编排、详情页、记录列表、目录缓存和 mock 数据边界，降低主流程复杂度。
- 引入 CSS 设计变量、通用 UI 样式和样式入口拆分，让主题色、布局和组件样式更集中。
- 复用依赖处理和通知工具逻辑，收敛错误提示与刷新流程，减少重复状态处理。
- 扫描时忽略 `Mods/Cache`，避免缓存文件夹污染本地内容结果。

### 修复

- 收敛数据刷新竞态，降低快速操作或并发刷新时的状态不一致风险。
- 补充接口契约、关键交互、关键布局、备份和下载回归测试，覆盖发布前风险点。
- 增加 CSS 检查，避免样式约束回退。

## v0.4.1 - 2026-06-06

### 改进

- 拆分 App 弹窗、依赖解析、下载任务控制 Hook、Mod 安装更新流程和 Tauri 命令入口，降低主组件与命令入口复杂度。
- 本地 Mod 详情显示被哪些地图或 Mod 依赖，便于判断禁用影响范围。

### 修复

- 禁用 Mod 失败时展示更明确的失败提示。
- 修复工具栏加载状态在窄宽度下换行导致布局跳动的问题。

## v0.4.0 - 2026-06-05

### 新增

- Mod 获取中心支持目录缓存刷新、来源筛选、类型筛选、安装状态识别和完整结果分页。
- 本地内容与目录结果共用分页控件，支持固定长度页码、省略号和每页 50 条展示。
- 新增启动时自动检查 Mod 更新和自动刷新目录缓存的默认配置。

### 改进

- 优化目录搜索匹配规则，减少由版本号、类型和 URL 造成的误匹配。
- 目录搜索与刷新不再在后端硬截断 200 条结果，前端负责完整筛选、排序和分页展示。
- 缓存已安装 zip Mod 的文件哈希，减少重复检查更新时的完整文件哈希计算。
- 统一 HTTP User-Agent 与超时设置，目录请求和下载请求使用当前包版本。
- 优化扫描缓存写入和 Everest build 读取，降低大文件扫描和缓存写入开销。
- mock 页面扩充本地内容和目录数据，便于验证分页、筛选和大列表展示。

### 修复

- 加固配置状态读改写，避免并发更新时互相覆盖。
- 修复 Profile 应用后重复读取状态导致的快照不一致风险。
- 修复下载暂存路径解析和取消标记清理逻辑，降低残留状态风险。
- 加固备份恢复路径、始终启用路径和目录安装状态匹配。
- 修复加载状态并发计数和前端格式检查问题。

## v0.3.2 - 2026-06-05

### 修复

- 修复发布前本地检查发现的问题。
- 修复目录排序等 Rust clippy 警告，确保 CI 的 Rust lint 步骤通过。
- 整理 Prettier 格式化结果，确保 CI 的格式检查通过。
- 补充发布流程文档，明确发布前需执行 CI 同款检查，并在推送 tag 前确认 main CI 通过。

## v0.3.1 - 2026-06-05

### 改进

- 统一备份范围口径：备份只覆盖 CelePkg 管理的启用状态文件，清单记录本地 Mod 名称、版本和启用状态，但不复制 Mod 文件本体。
- 统一应用内确认弹窗，覆盖删除 / 覆盖 Profile、删除 / 还原备份、安装 / 更新 Mod、安装 Everest 和依赖处理等确认流程。
- 左侧“地图与 Mod”改为“本地内容”，列表顶部明确显示当前为“地图”或“其他 Mod”，筛选标题会随视图显示“地图筛选”“Mod 筛选”或“小图筛选”。

### 修复

- 修复统一弹窗恢复焦点时的 React Hooks lint 警告。

## v0.3.0 - 2026-06-04

### 新增

- 新增 Mod 获取中心，支持目录源、搜索结果详情、更新检查、下载与安装。
- 新增 Everest 通道安装，并在安装前检查游戏文件占用。
- 新增 UI Mock 预览入口，便于在无 Tauri 后端时验证界面状态。
- 新增独立下载管理页，统一展示下载中、下载失败、等待安装、安装成功、安装失败五类任务。
- Mod 与 Everest 下载接入统一任务流程，支持并发下载、串行安装、失败重试、暂停/恢复下载与安装。
- 新增 staged 下载与安装流程，下载完成后再进入安装队列，便于取消、校验与依赖编排。

### 改进

- 统一 Mod 安装与更新流程：先下载目标项，进入安装队列后检查依赖，依赖完成后再继续安装目标项。
- 更新全部会按当前已安装依赖图生成安装顺序，运行时发现的新必须依赖会并入同一任务。
- Everest Core 依赖单独优先检测，用户可选择更新 Everest 后继续或忽略。
- Mod 数据源设置补充已启用顺序说明，并优化批量启用/禁用当前结果的语义。
- 地图与 Mod 筛选状态改为各自独立的临时状态，避免不同页面筛选条件互相影响。

### 修复

- 修复 Mod 下载进度只从 0% 跳到完成的问题，并兼容带 BOM 的 Mod 元数据。
- 修复下载取消后仍继续启动下一个 Mod 下载的问题。
- 修复依赖失败后目标项跳过、失败任务重试、更新全部依赖复用等任务调度问题。
- 修复 Everest 版本选择列表高度过小、下载管理空状态占位、下载管理列表显示等界面问题。
- 修复数据源选择器在开发环境拖动失效、拖出目标框释放后状态未更新的问题。
- 修复开启启动时检查更新后不会立即自动触发检查的问题。

## v0.2.0 - 2026-06-03

### 新增

- Profile 列表支持行内重命名，点击重命名按钮后可直接编辑，回车、再次点击保存或失焦都会保存。
- 备份管理支持删除单个备份、清理旧自动备份，并新增自动备份保留策略开关。
- 还原受管启用状态文件前新增预览确认，展示将覆盖、创建或删除的目标文件。
- 扫描 Mod 依赖时会提示可能过低的依赖版本，包括 Celeste、EverestCore 等内置依赖。

### 改进

- 自动备份清理只处理自动备份，手动备份不会被自动清理删除。
- 内置依赖无法确认版本时会合并为一条扫描级警告，减少重复噪音。
- Celeste 内置版本按常见版本 `1.4.0.0` 判断，避免常见依赖声明误报无法确认。

### 修复

- 修复 CI 中的 Rust lint 告警。

## v0.1.0 - 2026-06-03

### 新增

- 新增本地 Celeste 内容扫描，支持识别地图、其他 Mod、小图和官方地图。
- 新增地图 Profile 与 Mod Profile 管理，支持组合、复制、覆盖、启动和启用数量展示。
- 新增本地内容详情页，展示依赖、警告、版本、草莓统计、小图完成状态和难度信息。
- 新增收藏、始终启用、未被依赖 Mod 筛选、警告列开关和列表筛选记忆。
- 新增选择数字存档查看统计、官方地图中文名称读取和小图难度控制。
- 新增游戏文件备份与还原管理，支持还原确认和受管状态文件保护。
- 新增 Celeste 目录选择、扫描结果缓存、缓存重扫和扫描加载状态。
- 新增显示设置、问题面板、筛选折叠提示和主要列表/详情布局。
- 新增 API 响应校验、错误恢复、依赖别名标准化和 Tauri CSP 配置。
- 添加 CI 自动测试与三平台构建流程。
- 添加基于 tag 的自动化 GitHub Release 流程。

### 改进

- 优化主界面、Profile 视图、详情页标题区、小图浏览区和 Mod 概览布局。
- 保留列表和详情滚动位置，提升筛选、切换和返回时的连续性。
- 优化收藏状态更新速度、批量操作按钮布局和列表操作图标展示。
- 使用中文章节名展示官方地图，并从游戏 dialog 中读取官方地图名称。
- 将记录操作、备份操作和工作区视图状态拆分为独立前端 Hook，降低主界面复杂度。
- 使用枚举和原子写入增强后端状态、记录与 Mod 文件写入稳定性。

### 修复

- 修复小图完成状态识别、完成状态布局、表格列对齐和详情页子标签滚动遮挡。
- 修复禁用失败时依赖来源提示不清晰的问题。
- 修复 Hooks 依赖、TypeScript 未使用项、Rust clippy 和前端格式检查问题。
- 避免配置静默重置，并减少重复状态读取和受保护记录扫描。
